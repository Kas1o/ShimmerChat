@page "/contextmanager"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // ShimmerChat 2.0: redirect to new Generation Manager
        Navigation.NavigateTo("/generationmanager", replace: true);
    }
}
